import { Meta, Story } from "@storybook/addon-docs/blocks";
import { action } from "@storybook/addon-actions";
import { Button, Row } from "../../src/";

<Meta
  title="Button"
  component={Button}
  argTypes={{
    onClick: { action: "clicked" },
    color: {
      control: "color",
    },
  }}
/>

# Button

A button is the basic interative component in the library. The style property can be used to select between a button
and more traditional web link.

export const Template = (args) => (
  <div>
    <Row justifyContent="start">
      This is a normal button <Button {...args} />
    </Row>
    <Row justifyContent="start">
      This a link button <Button {...args} style="link" />
    </Row>
  </div>
);

<Story name="Button" args={{ name: "Press Me" }}>
  {Template.bind({})}
</Story>
